# =============================================================================
# FunnelSim Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values
# NEVER commit your actual .env file to version control
# =============================================================================

# -----------------------------------------------------------------------------
# Supabase Configuration (Required)
# -----------------------------------------------------------------------------
# Get these values from your Supabase project settings
VITE_SUPABASE_PROJECT_ID="your-project-id"
VITE_SUPABASE_PUBLISHABLE_KEY="your-anon-key"
VITE_SUPABASE_URL="https://your-project-id.supabase.co"

# -----------------------------------------------------------------------------
# Stripe Configuration (Required for payments)
# -----------------------------------------------------------------------------
# Get your publishable key from Stripe Dashboard > Developers > API keys
# Note: Use test keys for development (pk_test_...), live keys for production (pk_live_...)
VITE_STRIPE_PUBLISHABLE_KEY="pk_test_your_stripe_publishable_key"

# -----------------------------------------------------------------------------
# Whitelabel Branding (Optional - for custom deployments)
# -----------------------------------------------------------------------------
# These environment variables allow you to customize the branding of your
# FunnelSim deployment. If not set, defaults from the database whitelabel_config
# table will be used. Environment variables take precedence over database values.

# Brand name displayed in the app and landing page
VITE_BRAND_NAME="FunnelSim"

# Brand tagline displayed on the landing page
VITE_BRAND_TAGLINE="Visual Funnel Modeling Made Simple"

# Primary brand color (hex format, e.g., #6366f1 for indigo)
VITE_PRIMARY_COLOR="#6366f1"

# Logo URLs - provide full URLs to your logo images
# Light logo: displayed on dark backgrounds
VITE_LOGO_URL=""

# Dark logo: displayed on light backgrounds
VITE_LOGO_DARK_URL=""

# Favicon URL: browser tab icon
VITE_FAVICON_URL=""

# =============================================================================
# Supabase Edge Function Secrets
# =============================================================================
# These are NOT frontend environment variables. They must be configured
# as secrets in your Supabase project:
#
# Via Supabase Dashboard:
#   Project Settings > Edge Functions > Secrets
#
# Via Supabase CLI:
#   supabase secrets set STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
#   supabase secrets set STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret
#
# Required Edge Function Secrets:
# -----------------------------------------------------------------------------
# STRIPE_SECRET_KEY
#   Your Stripe secret API key from Dashboard > Developers > API keys
#   Use test key (sk_test_...) for development, live key (sk_live_...) for production
#
# STRIPE_WEBHOOK_SECRET
#   Webhook signing secret from Stripe Dashboard > Developers > Webhooks
#   Create a webhook endpoint pointing to:
#   https://your-project-id.supabase.co/functions/v1/stripe-webhook
#   Subscribe to these events:
#     - checkout.session.completed
#     - customer.subscription.updated
#     - customer.subscription.deleted
#     - charge.refunded
#
# Existing Edge Function Secrets (already configured):
# -----------------------------------------------------------------------------
# SUPABASE_URL - Automatically available in Edge Functions
# SUPABASE_SERVICE_ROLE_KEY - Automatically available in Edge Functions
# ELASTIC_EMAIL_API_KEY - For password reset emails (if using Elastic Email)
# =============================================================================
